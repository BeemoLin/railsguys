<!DOCTYPE html>
<html>
<head>
  <title>Railsguys</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

  <div class="navbar navbar-fixed-top navbar-inverse">
    <div class="navbar-inner">
      <div class="navbar-header">
        <a class="brand" href="#">Message Board</a>
      </div>
        <ul class="nav navbar-nav">
          <li class="active"><a href="/"><i class="icon-home icon-white"></i>Home</a></li>
        </ul>
        <p class="navbar-text pull-right">
          <% if user_signed_in? %>
            <%= link_to current_user.username, edit_user_registration_path, :class => "btn btn-default navbar-btn" %>
             | 
            <%= link_to "Sign Out", destroy_user_session_path, method: :delete, :class => "btn btn-default navbar-btn" %>
          <% else %>
            <%= link_to "Sign Up", new_user_registration_path, :class => "btn btn-default navbar-btn" %>

             | 
            <%= link_to "Sign In", new_user_session_path, :class => "btn btn-default navbar-btn" %>
          <% end %>
      </p>
    </div>
  </div>
  <div>
    <%= yield %>
  </div>
</body>
</html>
